

CREATE TABLE years (
    yaer_id INTEGER UNSIGNED PRIMARY KEY AUTO_INCREMENT NOT NULL,
    year INTEGER UNSIGNED 
);

CREATE TABLE states (
    state_id INTEGER UNSIGNED PRIMARY KEY AUTO_INCREMENT NOT NULL,
    state VARCHAR(250)
);

CREATE TABLE concepts (
    concept_id INTEGER UNSIGNED PRIMARY KEY AUTO_INCREMENT NOT NULL,
    concept VARCHAR(250)
);