

CREATE TABLE years (
    yaer_id INTEGER UNSIGNED PRIMARY KEY AUTO_INCREMENT ,
    year INTEGER UNSIGNED NOT NULL 
);

CREATE TABLE states (
    state_id INTEGER UNSIGNED PRIMARY KEY AUTO_INCREMENT ,
    state VARCHAR(250) NOT NULL
);

CREATE TABLE concepts (
    concept_id INTEGER UNSIGNED PRIMARY KEY AUTO_INCREMENT ,
    concept VARCHAR(250) NOT NULL
);